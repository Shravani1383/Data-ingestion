Create a full screen transition loading screen 
with a LIVE NODE MAPPING VISUALIZATION 
at the centre. This is not a progress bar — 
it is a visual intelligence map showing 
exactly which data inputs are generating 
which carbon footprint outputs in real time.

BACKGROUND: Dark navy (#0a1628) full screen.

TOP:
Pulsing green dot + "AI CARBON INTELLIGENCE 
MAPPING" in small red caps centered.
Below in white bold 22px:
"Connecting your data to your footprint"
Below in gray 13px:
"Watch how each data source builds 
your carbon picture"

---

CENTRE — THE LIVE NODE MAP:

Three column layout connected by 
animated flowing lines:

LEFT COLUMN — "DATA SOURCES"
Label in gray small caps at top.
Show 7 source nodes appearing 
one by one, each as a small 
rounded card:

Node 1: Outlook icon
"8,412 emails"
"Procurement + invoices"

Node 2: SharePoint icon
"156 documents"  
"ESG reports + contracts"

Node 3: Google Sheets icon
"14,203 rows"
"Supplier + cost data"

Node 4: WhatsApp icon
"267 images"
"Invoices + shipments"

Node 5: Energy icon (lightning bolt)
"Utility bills"
"Electricity + gas records"

Node 6: Fleet icon (truck)
"Fuel receipts"
"Vehicle + route data"

Node 7: HSBC icon (red)
"Transaction history"
"Banking data — exclusive"

Each node:
White background, subtle shadow,
icon on left, text on right,
12px font, rounded corners.
Nodes appear with a subtle 
pop animation one by one.

---

CENTRE COLUMN — "AI PROCESSING"
Label in gray small caps at top.
One large central processing node:
Red circle, HSBC logo inside,
pulsing glow animation,
text below: "Carbon Intelligence Engine"

Below the central node, 
three smaller processing chips 
appearing sequentially:

Chip 1: "Entity Resolution"
Chip 2: "Emission Categorisation"  
Chip 3: "Regulatory Mapping"

Each chip: dark card, 
white text, small spinning 
icon while active, 
green checkmark when complete.

---

RIGHT COLUMN — "CARBON OUTPUTS"
Label in gray small caps at top.
Show 5 output nodes appearing 
as connections complete:

Output Node 1: Red left border
"Scope 1 — 8,400 tCO₂e"
"Direct emissions mapped"
Lights up when Fleet + 
Energy nodes connect

Output Node 2: Amber left border
"Scope 2 — 11,200 tCO₂e"
"Purchased energy mapped"
Lights up when Utility 
bills node connects

Output Node 3: Gray left border
"Scope 3 — 48,200 tCO₂e"
"Supply chain mapped"
Lights up when Outlook + 
SharePoint + Sheets + 
WhatsApp connect

Output Node 4: Blue left border
"HSBC Rate Differential"
"$120K saving identified"
Lights up when HSBC 
transaction node connects

Output Node 5: Green left border
"Regulatory Alerts — 3 live"
"EU Taxonomy + ZDHC flagged"
Lights up last

Each output node:
White background, colored 
left border, bold metric 
on top, gray description below,
starts dark/inactive, 
brightens when connected.

---

THE ANIMATED CONNECTIONS:

Flowing animated lines run from 
left source nodes through the 
central processing node 
to right output nodes.

Line animation style:
Dotted line with a bright 
moving dot travelling along it —
like data physically flowing 
from source to output.
Line color matches the 
output node it feeds into:
Red lines → Scope 1
Amber lines → Scope 2
Gray lines → Scope 3
Blue lines → HSBC output
Green lines → Regulatory output

Connection sequence — 
lines appear one at a time:

Second 1: Outlook → Engine → Scope 3
Second 2: SharePoint → Engine → Scope 3
Second 3: Google Sheets → Engine → Scope 3
Second 4: WhatsApp → Engine → Scope 3
Second 5: Energy → Engine → Scope 2
Second 6: Fleet → Engine → Scope 1
Second 7: HSBC → Engine → HSBC Rate
Second 8: All sources → Engine → Regulatory

As each connection completes:
- Output node lights up 
- Metric number counts up 
  to final value
- Line stays visible but dims
  slightly so new lines stand out

---

BOTTOM SECTION:

As final connection completes,
all output nodes are bright,
show a summary bar appearing:

"Total footprint identified: 
67,800 tCO₂e across 3 scopes
| 4 connected systems 
| 12 emission categories"

In white centered text, 
then red CTA button fades in:
"View Carbon Footprint Report →"

---

TOTAL TIMING: 8–10 seconds
Left nodes appear: seconds 0–2
Connection lines flow: seconds 2–8
Output nodes light up: seconds 2–8
Summary + CTA: seconds 9–10

FEEL: Like watching a live 
intelligence graph build itself.
Every connection tells a story —
this data produced this insight.
Nothing feels arbitrary.
Bloomberg terminal meets 
AI neural network visualization.
